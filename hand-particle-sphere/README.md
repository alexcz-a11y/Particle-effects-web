# 🌀 Hand Gesture Particle Sphere

基于 WebGPU 的手势控制粒子球特效，使用 Three.js r171 和 MediaPipe Hands 实现。

![WebGPU](https://img.shields.io/badge/WebGPU-Native-blue)
![Three.js](https://img.shields.io/badge/Three.js-r171-green)
![MediaPipe](https://img.shields.io/badge/MediaPipe-Hands-orange)

## ✨ 特性

- **WebGPU 原生渲染** - 利用 Apple M 芯片 GPU 原生加速
- **实时手势交互** - 通过摄像头识别手势控制粒子
- **TSL 着色器** - 使用 Three.js Shading Language 实现程序化网格和反射
- **HDR Bloom 后处理** - 粒子具有强烈的发光效果
- **实时镜面反射** - 地板实时反射粒子球

## 🎮 交互方式

| 手势 | 效果 |
|------|------|
| ✊ **握拳** | 粒子聚合成球体 |
| 🖐 **张开手掌** | 粒子向四周扩散 |
| 👋 **移动手掌** | 控制整个空间旋转 |
| 🚫 **无手势** | 自动缓慢旋转 |

## 🛠 技术栈

- **渲染引擎**: Three.js r171 (WebGPU)
- **着色器**: TSL (Three.js Shading Language)
- **手势识别**: MediaPipe Hands
- **后处理**: Bloom 发光效果

## 📁 项目结构

```
hand-particle-sphere/
├── index.html      # 主页面，包含 import map 配置
├── script.js       # 主逻辑脚本
└── README.md       # 本文档
```

## 🚀 快速开始

### 环境要求

- 支持 WebGPU 的浏览器（Chrome 113+, Edge 113+, Safari 17+）
- 摄像头访问权限
- 本地 HTTP 服务器（ES Modules 需要）

### 运行步骤

1. **克隆项目**
   ```bash
   cd hand-particle-sphere
   ```

2. **启动本地服务器**
   ```bash
   # Python 3
   python3 -m http.server 8000
   
   # 或使用 Node.js
   npx serve .
   ```

3. **访问页面**
   ```
   http://127.0.0.1:8000
   ```

4. **允许摄像头权限**，将手掌放入摄像头视野中

## ⚙️ 配置参数

在 `script.js` 顶部可调整以下参数：

```javascript
const PARTICLE_COUNT = 3000;      // 粒子数量
const SPHERE_RADIUS = 2.5;        // 球体半径
const DISPERSION_FACTOR = 15;     // 扩散范围
const LERP_SPEED = 0.05;          // 动画平滑度
```

### Bloom 效果参数

```javascript
bloomPass.threshold.value = 0.15; // 发光阈值
bloomPass.strength.value = 3.5;   // 发光强度
bloomPass.radius.value = 0.8;     // 发光半径
```

## 🎨 视觉效果说明

### 粒子系统
- 使用 Fibonacci 球面分布算法均匀分布粒子
- HDR 颜色值（RGB > 1.0）实现强烈发光
- 扩散时采用高斯分布，中心密度更高

### 地板反射
- 使用 TSL `reflector()` 实现实时 GPU 反射
- 边缘渐变淡出，避免生硬截断
- 反射强度降低至 30%，避免过度 Bloom

### 程序化网格
- 纯 TSL 实现，无额外几何体
- 从中心向边缘渐变消失
- 深蓝色调与粒子统一

## 🔧 浏览器兼容性

| 浏览器 | 支持情况 |
|--------|----------|
| Chrome 113+ | ✅ 完全支持 |
| Edge 113+ | ✅ 完全支持 |
| Safari 17+ | ✅ 完全支持 (macOS) |
| Firefox | ❌ 暂不支持 WebGPU |

## 📝 注意事项

1. **WebGPU 必需** - 本项目不支持 WebGL 回退
2. **摄像头权限** - 必须允许摄像头访问才能使用手势控制
3. **HTTPS/localhost** - 摄像头 API 需要安全上下文
4. **性能** - 在 Apple M 芯片设备上表现最佳

## 🤖 Vibe Coding

本项目采用 **Vibe Coding** 方式开发，全程通过自然语言对话驱动代码生成与优化。

### 使用的 AI 工具

| 工具 | 模型 |
|------|------|
| **Windsurf IDE** | Cascade AI Agent |
| **代码生成** | Gemini 3 Pro |
| **代码优化** | Claude Opus 4.5 |

> 💡 Vibe Coding：通过与 AI 对话描述需求和效果，让 AI 自动生成、调试和优化代码的编程方式。

## 📄 License

MIT License

---

Made with ❤️ using Three.js WebGPU + MediaPipe

🤖 Powered by Vibe Coding with Windsurf + Gemini 3 Pro + Claude Opus 4.5
